interface:
  display_name: "Video Generation"
  short_description: "Generate short videos through the shared MCP"
  default_prompt: "Use $video-generation to create the requested short video after confirming costly settings."
